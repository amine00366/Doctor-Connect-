{% extends 'base.html.twig' %}

{% block title %}Fiche{% endblock %}

{% block body %}
 
  

   {% block stylesheets %}
    {{ parent() }}
    <style>
      body {
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        padding-top: 50px;
        padding-bottom: 50px;
        
      } 
      .user-avatar {
  width: 40px;
  height: 40px;
}
.custom-navbar   {
  padding-top: 50px;
        padding-bottom: 50px;
  width: 800px;
  height: 70px;
  margin: 2 auto;
   font-size: 18px;
  color: #333;
}
 .h3{font-size: 3px;
  color: #333;}

      
       </style>
{% endblock %}
 
    <div class="  p-1 text-center"> <h1  class=" mb-3 text-primary ">Fiche Patient</h1> </div>
 <div class="d-flex justify-content-center align-items-center">
    <table class="table table-striped mx-auto">
        <tbody>
            <tr>
                <th>Id Fiche</th>
                <td>{{ fiche.id }}</td>
            </tr>
           
            <tr>
                <th>Id Consultation</th>
               <td>{{ fiche.getIdConsultation() }}</td>
            </tr>
            <tr>
                <th>Créé pa le Docteur.</th>
                <td>{{ fiche.getIdConsultation().getIdMedcin().getNom()}} {{ fiche.getIdConsultation().getIdMedcin().getPrenom()}}</td>
            </tr>
            <tr>
            <th>Le Nom & Le Prenom du Patient</th>
                <td>
             {{ fiche.getIdConsultation().getIdUser().getNom()}} {{ fiche.getIdConsultation().getIdUser().getPrenom()}} 
             </td></tr>
            <th>Cin</th>
                <td>{{ fiche.getIdConsultation().getIdUser().getCin()}}</td>
            </tr>
            <th>Age</th>
                <td>{{ fiche.getIdConsultation().getIdUser().getAge()}}</td>
            </tr>
            <th>Adresse</th>
                <td>{{ fiche.getIdConsultation().getIdUser().getAdresse()}}</td>
            </tr>
           
                
                <tr>
                <th>Note</th>
                <td>{{ fiche.note }}</td>
            </tr>
          
        </tbody>
    </table>
</div>
    <a href="{{ path('app_fiche_index') }}">Retourner à la liste</a>

    <a href="{{ path('app_fiche_edit', {'id': fiche.id}) }}">edit</a>

    {{ include('fiche/_delete_form.html.twig') }}
 
{% endblock %}
