<form method="post" action="{{ path('app_ordonnance_delete', {'id': ordonnance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ordonnance.id) }}">
    <button class="btn btn btn-danger">
    <img src="/assets2/img/suppr.png"  style="width :18px" alt="" ></button>
</form>
