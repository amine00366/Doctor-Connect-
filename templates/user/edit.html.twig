{% extends 'base.html.twig' %}
{% block body %}
<div class="container">
  <div class="row justify-content-center">
    <div class="col-lg-6">
      <div class="card mt-5">
        <div class="card-header">
          <h1 class="text-center">Edit User</h1>
        </div>
        <div class="card-body">
          
            {{ include('registration/editandreg.html.twig', {'button_label': 'Update', 'button_class': 'btn-danger'}) }}
 
          <div class="d-flex justify-content-between">
            <a href="{{ path('app_user_index', {'id': user.id}) }}"class="btn btn-success">
              <i class="fa fa-arrow-left"></i>
              <span class="ml-2">Go Back</span>
            </a>
            {{ include('user/delete.html.twig') }}
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
{% endblock %}