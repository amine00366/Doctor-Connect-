{% extends 'admin.html.twig' %}

{% block title %}Reclamation{% endblock %}

{% block body %}
<div style="margin-left: 280px; width: 80% ">

    <h1>Reclamation</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Date</th>
                <td>{{ reclamation.date ? reclamation.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ reclamation.email }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ reclamation.telephone }}</td>
            </tr>
            <tr>
                <th>Cmnt</th>
                <td>{{ reclamation.cmnt }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ reclamation.etat }}</td>
            </tr>
        </tbody>
    </table>
<!-- Button trigger modal -->


<br><br>
    <a class="btn btn-primary" href="{{ path('app_rec_admin_index') }}">back to list</a>
    <a class="btn btn-primary" href="{{ path('app_rec_admin_edit', {'id': reclamation.id}) }}">edit</a>

	</div>
    {{ include('rec_admin/_delete_form.html.twig') }}
    </div>

</div>
    {% endblock %}




