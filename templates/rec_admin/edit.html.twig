{% extends 'admin.html.twig' %}

{% block title %}Edit Reclamation{% endblock %}

{% block body %}
<div style="margin-left: 280px; width: 80% ">
    <h2>Vous pouvez changer l'état de traitement de la réclamation</h2>
    {{ include('rec_admin/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-primary" class="btn btn-info" href="{{ path('app_rec_admin_index') }}">back to list</a>

    {{ include('rec_admin/_delete_form.html.twig') }}
</div>
{% endblock %}
