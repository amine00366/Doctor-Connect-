{% extends 'base.html.twig' %}

{% block title %}TypeReclamation{% endblock %}

{% block body %}
    <h1>TypeReclamation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ type_reclamation.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_type_reclamation_index') }}">back to list</a>

    <a class="btn btn-warning" href="{{ path('app_type_reclamation_edit', {'id': type_reclamation.id}) }}">edit</a>

    {{ include('type_reclamation/_delete_form.html.twig') }}
{% endblock %}
