<form method="post" action="{{ path('app_type_rec_admin_delete', {'id': type_reclamation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_reclamation.id) }}">
    <button class="btn btn-primary">Delete</button>
</form>
