{% extends 'base.html.twig' %}

{% block title %}New Consultation{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-lg-8">
    
        <div class="card">
            <div class="card-body">
 <h1 class="mb-3 text-primary text-center my-105">Create new Consultation</h1>

    {{ include('consultation/_form.html.twig') }}

    <a href="{{ path('app_consultation_index') }}">back to list</a>
    </div>
        </div>
    </div>
</div>

{% endblock %}
